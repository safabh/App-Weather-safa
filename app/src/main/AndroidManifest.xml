<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplicationtest">

    <application
        android:name=".myapplicationtest"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationTest">

        <activity
            android:name=".ui.activity.WeatherDetailsActivity"
            android:label="@string/Check_Your_Weather" />
        <activity android:name=".ui.activity.AddNewCityActivity"
            android:label="@string/Add_City"/>
        <activity android:name=".ui.activity.MainActivity"
            android:label="@string/List_Cities"/>
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>